version: 1

build:
  type: Release
  dir: build-contract-tests
  install_dir: .hako/install-contract-tests
  shared: false

bindings:
  python: false

validation:
  tests: true
  python_import: false

paths:
  pdu_endpoint_root: ""
  vcpkg_root: ""
